#!/bin/sh -e

logger -t "weatherstation.observe-view" "confdb weather view changed; (re)starting daemon"

# (Re)start unconditionally: the first committed configuration must be able to
# bring up a daemon that is currently inactive (it exits while unconfigured).
snapctl restart weatherstation.weatherstationd || true

exit 0
